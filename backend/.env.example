# Copy this file to .env and fill in your values.
# docker-compose uses the values baked into docker-compose.yml,
# so .env is only needed when running the API outside of Docker.
DATABASE_URL=postgresql://streetgolf:streetgolf@localhost:5432/streetgolf
